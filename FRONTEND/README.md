# Frontend - Contactos

Aplicación React con Bootstrap. La validación, el guardado, la edición y la eliminación se hacen con jQuery, como pide la prueba.

## Arranque

```bash
npm install
npm run dev
```

Abre http://localhost:5173

El frontend espera el backend en http://localhost:3000 (Vite reenvía `/api`).
